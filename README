##use Rsa to encrypt or decrypt all files unber a directory "./data"

#requirement
pip install pycryptodome

#encrypt files under data
python endecrypt -m e -p data
#decrypt files under data
python endecrypt -m d -p data
